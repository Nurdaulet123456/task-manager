import styled from "@emotion/styled";

const _Between = styled.span`
  border-left: 1px solid #fff;
  height: 16px;
  margin-inline: .8rem;
  margin-block: auto;
  display: inline-block;
`;

export const Between = () => {
  return <_Between />;
};
